export type StudentInfo = {
  name: string;
  class: string;
  Chinese: number;
  Maths: number;
  English: number;
  Chemical: number;
};

export type HttpErrorResponse = {
  msg: string,
  statusCode: number
}
